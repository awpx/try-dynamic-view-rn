import React, {useState, useEffect} from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Details = () => {

  return (
    <View>
      <Text>Tis is Details</Text>
    </View>
  )
}

export default Details

const styles = StyleSheet.create({})
